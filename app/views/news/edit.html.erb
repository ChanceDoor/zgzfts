<h1>修改</h1>
 
<%= form_for @news,:method => :put do |f| %>
  <% if @news.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @news.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label '标题' %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label '内容' %><br>
    <%= f.kindeditor :body %>
  </p>
 
  <p>
    <%= f.submit '提交' %>
  </p>
<% end %>
 
<%= link_to '返回', admin_root_path %>
