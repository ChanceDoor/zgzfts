<%= form_for @post do |f| %>
  <% if @post.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label '标题' %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label '内容' %><br>
    <%= f.kindeditor :body %>
  </p>
 
  <p>
    <%= f.label '作者' %><br>
    <%= f.select :editor_id, options_from_collection_for_select(@editors, "id", "name") %>
  </p>

  <p>
    <%= f.submit '提交'%>
  </p>
<% end %>
 
<%= link_to '返回', admin_root_path %>
